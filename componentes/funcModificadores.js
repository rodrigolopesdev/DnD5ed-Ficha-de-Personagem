function modForca(valNum) {
    valNum = parseFloat(valNum)
    document.getElementById("outputForca").innerHTML=Math.floor((valNum-10)/2)
}

function modDestreza(valNum) {
    valNum = parseFloat(valNum);
    document.getElementById("outputDestreza").innerHTML=Math.floor((valNum-10)/2)
}

function modSabedoria(valNum) {
    valNum = parseFloat(valNum);
    document.getElementById("outputSabedoria").innerHTML=Math.floor((valNum-10)/2)
}

function modInteligencia(valNum) {
    valNum = parseFloat(valNum);
    document.getElementById("outputInteligencia").innerHTML=Math.floor((valNum-10)/2)
}

function modConstituicao(valNum) {
    valNum = parseFloat(valNum);
    document.getElementById("outputConstituicao").innerHTML=Math.floor((valNum-10)/2)
}

function modCarisma(valNum) {
    valNum = parseFloat(valNum);
    document.getElementById("outputCarisma").innerHTML=Math.floor((valNum-10)/2)
}
